<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="../commands.pack.targets" />

  <PropertyGroup>
    <Version>0.1.3</Version>
    <Nullable>enable</Nullable>
    <TargetFramework>net7.0</TargetFramework>
    <PackageId>Atmoos.Progress.Reporting</PackageId>
    <Description>Library of commonly used transformations and filtering of progress reports exposed as extension methods.</Description>
    <PackageTags>progress, IProgress, IProgress&lt;T&gt;, report progress, progress reporting, progress filter, progress filtering, progress map, progress mapping</PackageTags> 
  </PropertyGroup>

</Project>
